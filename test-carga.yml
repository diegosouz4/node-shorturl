config:
  target: "http://localhost:5000"
  phases:
    - duration: 20
      arrivalRate: 100
scenarios:
  - flow:
    - post:
        url: '/api/v1/urls'
        json:
          url: 'https://dsouza.com.br'
